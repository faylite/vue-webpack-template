/**
 * Example filter for showing how to structure your filters in this template
 *
 * @param {String} value - Some string value
 */
export function filterMethod (value) {
  return `'${value}' was processed by the example filter`
}
